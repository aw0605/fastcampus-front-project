{
  "name": "front-projects",
  "version": "0.0.0",
  "workspaces": [
    "packages/**/*",
    "services/**/*"
  ],
  "scripts": {
    "build": "nx build",
    "graph": "nx run-many --target=build --graph"
  },
  "packageManager": "yarn@4.4.1",
  "devDependencies": {
    "nx": "16.8.1"
  }
}
